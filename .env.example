# 服务端口（本地开发）
PORT=3001

# Docker 部署对外端口
DOCKER_PORT=15000

# 管理员密码（默认123456，请自行修改）
ADMIN_PASSWORD=123456

# JWT密钥（建议修改为随机字符串）
JWT_SECRET=uploader-secret-key-2024

# 分片大小（MB），默认5MB
CHUNK_SIZE=5

# 数据存储目录（Docker挂载用，默认为/app/data）
DATA_DIR=/app/data
